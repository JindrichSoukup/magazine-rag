# Versions are pinned to what the Živa archive (75 issues) actually ran
# on. Without pinning, the pipeline behaves differently on every
# machine: PyMuPDF changes how it splits a page into blocks between
# versions, and that is the input to everything else.

# --- PDF extraction (stages 0-3) ------------------------------------------
PyMuPDF==1.25.3

# --- embedding (stage 5) --------------------------------------------------
# torch pulls ~2 GB; a CPU is enough, no GPU needed. If you only want to
# study the PDF extraction, PyMuPDF above is all you need.
sentence-transformers==5.6.0
transformers==5.14.1
torch==2.13.0
numpy==1.26.4

# --- vector store and retrieval (stage 6) ---------------------------------
chromadb==1.5.9

# --- answer generation (stage 7) ------------------------------------------
anthropic>=1.0

# --- optional -------------------------------------------------------------
# tiktoken: exact token counts in assemble_context/answer. Without it the
# count is estimated from characters, and the scripts say so themselves.
tiktoken
tqdm==4.66.5
